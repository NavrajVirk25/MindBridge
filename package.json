{
  "name": "mindbridge",
  "version": "1.0.0",
  "description": "A mental health support platform for universities",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["mental-health", "university", "pwa", "react", "nodejs"],
  "author": "MindBridge Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

